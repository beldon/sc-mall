server:
  port: 3300

spring:
  application:
    name: order
  cloud:
    consul:
      host: ${consul-host:127.0.0.1}
      port: ${consul-port:8500}
      discovery:
        prefer-agent-address: true